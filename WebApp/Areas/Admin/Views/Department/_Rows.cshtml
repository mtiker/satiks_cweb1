@model IReadOnlyList<WebApp.Areas.Admin.ViewModels.Department.DepartmentListItem>
@foreach (var item in Model)
{
    <tr>
        <td>@item.Name</td>
        <td class="text-muted small">@item.CostCenter</td>
        <td class="text-muted">@item.LaboratoryCount</td>
        <td class="text-muted small">@item.Description</td>
        <td class="text-end">
            <div class="btn-group btn-group-sm">
                <a asp-area="Admin" asp-controller="Department" asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-primary">Details</a>
                <a asp-area="Admin" asp-controller="Department" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-secondary">Edit</a>
                <a asp-area="Admin" asp-controller="Department" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger">Delete</a>
            </div>
        </td>
    </tr>
}
